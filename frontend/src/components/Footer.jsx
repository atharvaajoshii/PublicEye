import React from "react";
import { useNavigate } from "react-router-dom";

function Footer({ issue }) {
  const navigate = useNavigate();

  return (
    <div>
      
    </div>
  );
}

export default Footer;